# fastblock-exporter

This module contains the northbound exporter manager that will translate CSI publish requests into SPDK NVMe-oF export operations.

Planned binary:
- `cmd/fastblock-exporter`

Current status:
- repository skeleton only
- no RPC server wired yet
- scoped to SPDK NVMe-oF export orchestration
